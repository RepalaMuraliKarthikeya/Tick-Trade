import type { Ticket, User } from './types';

// Mock data is no longer needed as we are using Firestore.
// Keeping the file to avoid breaking imports, but with empty arrays.

export const mockUsers: User[] = [];

export const mockTickets: Ticket[] = [];
